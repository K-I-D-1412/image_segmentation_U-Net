# image_segmentation_U-Net
This project leverages a deep learning model to automatically identify balloon regions within images, achieving high-precision segmentation results. We trained a U-Net model that demonstrates superior performance by attaining high Intersection over Union (IoU) scores on both the validation and test sets.
